After installing Vagrant and VirtualBox on your host system, navigate to a directory containing `Vagrantfile` and `start.sh` and run

```bash
vagrant up
vagrant ssh
```

to let your set up the required VM for compiling and training Remy
